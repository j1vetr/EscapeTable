# EscapeTable Production Environment Variables
# Sunucuda .env dosyası oluşturun ve bu değerleri girin

NODE_ENV=production
PORT=9344

# PostgreSQL Database
# Kendi database bilgilerinizi girin
DATABASE_URL=postgresql://kullanıci:sifre@localhost:5432/escapetable

# Session Secret
# Güçlü bir rastgele string oluşturun:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=super-secret-random-string-buraya-yazin

# Domain (Production)
REPL_SLUG=escapetable
REPLIT_DOMAINS=escape.toov.com.tr

# Optional: Object Storage (eğer kullanıyorsanız)
# PUBLIC_OBJECT_SEARCH_PATHS=
# PRIVATE_OBJECT_DIR=
